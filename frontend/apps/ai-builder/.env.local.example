# Ollama API key — copy to .env.local and set your key
# Key is server-only (never sent to the browser)
OLLAMA_API_KEY=your_ollama_api_key_here

# auth-hub integration
AUTH_HUB_URL=http://localhost:3002
NEXT_PUBLIC_AUTH_HUB_URL=http://localhost:3002
AUTH_HUB_SERVICE_SECRET=smoke-test-secret
